name: jepa-infrastructure-smoke
seed: 4551
steps: 20

# CPU-only infrastructure smoke config. The GPU/data pipeline smoke uses
# baseline_smoke.yaml and configs/data/grandtour_smoke.yaml.
data_root: ${QUADWM_DATA_ROOT:-/path/to/external/data}
run_root: ${QUADWM_RUN_ROOT:-runs}

wandb:
  enabled: ${QUADWM_WANDB_ENABLED:-false}
  project: quad-wm
  name: jepa-smoke
  mode: ${WANDB_MODE:-offline}

name: jepa-infrastructure-smoke
seed: 4551
steps: 20

# External roots are intentionally supplied by the shell environment or Slurm.
data_root: ${QUADWM_DATA_ROOT:-/path/to/external/data}
run_root: ${QUADWM_RUN_ROOT:-runs}

wandb:
  enabled: false
  project: quad-wm
  name: jepa-smoke
  mode: ${WANDB_MODE:-offline}

wm:
  name: jepa
  observation: depth_plus_proprioception
  platform: anymal_d
